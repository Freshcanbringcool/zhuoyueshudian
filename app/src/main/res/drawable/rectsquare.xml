<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/middle_color">

    </solid>
    <!-- 边框的颜色和粗细 -->
    <stroke
        android:width="1dp"
        android:color="@color/font_grey"
        />

    <!-- android:radius 圆角的半径 -->
    <corners
        android:radius="0dp"
        />
</shape>